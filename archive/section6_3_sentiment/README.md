# Section 6.3 sentiment outputs — archive

Snapshot copies of `classifier.py` dev/test prediction CSVs (and the short results note), kept so `predictions/` can be overwritten by later runs.

| Folder | Contents |
|--------|----------|
| `2026-05-12_outputs/` | Eight `*-{dev,test}-out.csv` files, `predictions/README`, `section6_3_sentiment_classification.md` |

To archive a new run: create a dated subfolder and `cp predictions/*.csv` (and optional doc) into it.
